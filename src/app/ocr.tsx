// This file is intentionally left blank or removed.
// The OCR screen component has been moved to `src/app/[locale]/ocr.tsx`
// to align with the Expo Router `src/app/[locale]/` layout and `next-intl` setup.
// This file should not exist as a route or component at the root `src/app/` level
// to avoid conflicts and maintain a clean, locale-aware routing structure.
// All screen/route files MUST go in "src/app/[locale]/".
export default function RemovedOCRScreenComponent() {
  return null;
}

