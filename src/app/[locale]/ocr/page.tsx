```typescript
import OCRScreen from "../ocr";

export default function OCRPage() {
  return <OCRScreen />;
}
```
No deviations. This file correctly imports and renders the `OCRScreen` component, following the Next.js App Router pattern for locale-prefixed routes.
